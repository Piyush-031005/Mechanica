"use client";

import { useStore } from "@/store/useStore";

// The GridSystem no longer renders a large glowing floor.
// The architectural shaft rings in ArchiveHall now serve as the spatial reference.
// This file is intentionally empty — kept for import compatibility.
export function GridSystem() {
  return null;
}
